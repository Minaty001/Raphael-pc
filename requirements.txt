# Raphael v3 Always-Alive Runtime — Python backend dependencies
# Install:  pip install -r requirements.txt  (prefer a venv or uv)

fastapi==0.141.1
uvicorn==0.52.3
websockets==17.0.1
psutil==7.2.2
pydantic==2.13.4
requests==2.33.0
httpx==0.28.1
openai==2.24.0
python-dotenv==1.2.2
pillow==12.3.0
edge-tts==7.2.7
python-multipart==0.0.32
pydantic-core==2.46.4

# Optional (enable voice / wake-word / vision when available on your system):
#   vosk            - offline wake-word + STT
#   sounddevice     - microphone capture
#   pyttsx3         - offline TTS fallback
#   pystray         - system tray icon (RaphaelTray)
#   schedule        - task scheduler helpers
#   numpy / scikit-learn - embeddings / learning loop

# Raphael v3 Always-Alive Runtime — Python backend dependencies
# Install:  pip install -r requirements.txt  (prefer a venv or uv)

fastapi==0.141.1
uvicorn==0.52.3
websockets==17.0.1
psutil==7.2.2
pydantic==2.13.4
requests==2.33.0
httpx==0.28.1
openai==2.24.0
python-dotenv==1.2.2
pillow==12.3.0
edge-tts==7.2.7
python-multipart==0.0.32
pydantic-core==2.46.4

# Voice capture + offline wake-word/STT (core "Always-Alive" features, not optional).
# Note: `vosk` needs a downloaded model — set VOSK_MODEL_PATH (see README). The
# provider degrades to a clear warning if the model folder is missing.
sounddevice==0.5.1
vosk==0.3.45

# Optional (enable when available on your system):
#   pyttsx3         - offline TTS fallback
#   pystray         - system tray icon (RaphaelTray)
#   schedule        - task scheduler helpers
#   numpy / scikit-learn - embeddings / learning loop
#   pvporcupine     - low-latency wake-word KWS (alternative to transcript path)
#   playwright      - real browser DOM automation (browser_navigate/click/fill/
#                     extract tools). Install with: pip install playwright &&
#                     playwright install chromium. Without it, read_webpage still
#                     works (stdlib) and the DOM tools return an honest
#                     "engine not available" result.
